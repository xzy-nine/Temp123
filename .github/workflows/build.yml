name: 构建 Temp123 React 组件

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: 构建 React 组件
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Node.js 环境
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          cache: 'npm'

      - name: 生成 package-lock.json 文件
        run: npm install --package-lock-only

      - name: 安装依赖
        run: npm install

      - name: 构建项目
        run: npm run build

      - name: 验证构建产物
        run: |
          if [ -d "dist" ]; then
            echo "✅ 构建目录存在"
            ls -la dist/
          else
            echo "❌ 构建目录不存在"
            exit 1
          fi

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: temp123-build
          path: dist/
          retention-days: 30

      - name: 输出构建结果
        run: |
          echo "## 🔍 构建结果摘要" >> $GITHUB_STEP_SUMMARY
          echo "| 项目 | 状态 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| 依赖安装 | ✅ 完成 |" >> $GITHUB_STEP_SUMMARY
          echo "| 构建过程 | ✅ 完成 |" >> $GITHUB_STEP_SUMMARY
          echo "| 构建产物 | ✅ 已上传 |" >> $GITHUB_STEP_SUMMARY
          echo "| 构建路径 | dist/ |" >> $GITHUB_STEP_SUMMARY